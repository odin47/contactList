import React from 'react';
import './styles.scss';

const Header = () => {
	return (
		<header data-testid="header-id">
			<span>
				Contact List
			</span>
		</header>
	)
}

export default Header
